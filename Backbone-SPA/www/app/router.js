var Router  =  Backbone.Router.extend({	    		initialize:function(){	},		routes : {		'' : 'defaultRoute',		'homePage' : 'displayHome'	},		loginView : "",		homeView : "",		defaultRoute : function() {		if(this.homeView != ""){			this.homeView.remove();			this.homeView = "";		}		this.loginView = new Login_view();		this.loginView.render();		$("#template").html($(this.loginView.el));	},		displayHome : function(){		if(this.loginView != ""){			this.loginView.remove();			this.loginView = "";		}		this.homeView = new Home_view();		this.homeView.render();		$("#template").html($(this.homeView.el));	}});console.log("before new of router");appRouter = new Router();Backbone.history.start();				